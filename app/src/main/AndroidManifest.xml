<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.zky.zky">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/z"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">

        </activity>
        <activity android:name=".activities.introduction.DefaultIntroActivity"></activity>
        <activity
            android:name=".BaseThemeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".widget.surfaceview.SurfaceViewActivity" />
        <activity android:name=".widget.recyclerview.RecyclerViewActivity" />
        <activity android:name=".widget.WidgetActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".activities.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ActivitiesActivity"/>
    </application>

</manifest>